from .Dataloader import load_dataset
from .PDE import load_PDE

"""def callPDE(PDE_name):
    if PDE_name == '1DPoisson':
        from .1DPoisson import operator, f, u_exact, load_data
        return operator, f, u_exact, load_data
    elif PDE_name == '2DPoisson':
        from .2DPoisson import operator, f, u_exact, load_data
        return operator, f, u_exact, load_data
    elif PDE_name == '3DPoisson':
        from .3DPoisson import operator, f, u_exact, load_data
        return operator, f, u_exact, load_data"""
    
